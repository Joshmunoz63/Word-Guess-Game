// Words to pick from
var words = ["mazda", "nissan", "ford", "dodge", "jeep", "porsche", "honda"];

// This is what selects the random word
var word = words[Math.floor(Math.random() * words.length)];

console.log(word)

// creates the right amount of underscores for the word picked
var underscoreArray = [];  {
    for (var i = 0; i < words.length; i++) {
        underscoreArray.push("_");
        
    }
}

document.addEventListener(onkeyup, (event))  {
    console.log(event);
};

